[Unit]
Description=YARN Resourcemanager Service

[Service]
Type=simple
User={{ hadoop_user }}
Group={{ hadoop_user }}
ExecStart={{ hadoop_path_install }}/bin/yarn resourcemanager
StandardOutput=journal
StandardError=journal
Restart=always

[Install]
WantedBy=multi-user.target
